// 택시 기하학
#define _USE_MATH_DEFINES
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	double rad, s1, s2;
	cin >> rad;
	cout << fixed;
	cout.precision(6);
	cout << rad *rad* M_PI << '\n' << rad*rad * 2 << '\n';
}
